datasource db {
  provider = "postgresql"
}

generator client {
  provider     = "prisma-client-js"
  output       = "../generated/prisma"
}

model SuperAdmin {
  id           String    @id @default(uuid())
  email        String    @unique
  fullName     String
  passwordHash String
  status       String    @default("active") // "active" | "suspended"
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  auditLogs    SuperAdminAuditLog[]
}

model LguTenant {
  id           String    @id @default(uuid())
  code         String    @unique // e.g., "taytay", "sanjuan" (matches the LGU code in tenant-api)
  name         String
  level        String    // "province" | "city" | "municipality" | "barangay"
  apiUrl       String?   // The endpoint where this LGU's tenant-api is hosted (for monitoring & configuration)
  status       String    @default("active") // "active" | "suspended" | "maintenance"
  licenseKey   String?   @unique
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
}

model OnboardingRequest {
  id            String    @id @default(uuid())
  orgCode       String    @unique
  orgName       String
  level         String    // "province" | "city" | "municipality" | "barangay"
  sysAdminEmail String
  status        String    @default("pending") // "pending" | "provisioning" | "completed" | "failed"
  errorMessage  String?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
}

model SuperAdminAuditLog {
  id         String     @id @default(uuid())
  actorId    String
  actor      SuperAdmin @relation(fields: [actorId], references: [id])
  action     String     // e.g., "onboard_lgu", "suspend_lgu", "generate_license"
  details    String?    // Detailed description of the action
  ipAddress  String?
  userAgent  String?
  createdAt  DateTime   @default(now())
}
